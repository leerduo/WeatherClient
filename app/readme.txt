2015-05-07
上午：
数据来源：百度开放平台---车联网API
fastjson解析服务器端返回回来的json数据;Glide加载网络图片;网络请求;
遇到的问题:
fastjson在解析json的时候,实体类不能以内部类的形式存在，开始的时候我是以内部类的形式写的(json数据的格式)，然后报了这样的一个错误：
*create instance error*
这是一个，此外，网络请求服务器的数据放在子线程中去操作，当拿到数据后，不能在子线程中更新数据，只能在主线中更新。
下午：
获取省市县三级数据(由于数据来源不一致，可能有的城市百度车联网api没有数据，这时候程序会直接挂掉。加个判断即可(error和status的状态)),这部分还没做。
根据地点显示相关数据(Fragments之间的数据传递，这里只做了简单的处理，可以使用setArguments(bundle))。
加了SwipeRefreshLayout下拉刷新的控件。
晚上：
添加城市的时候，可以左右滑动切换（ViewPager，处理了一部分）
2015-05-08
上午：
